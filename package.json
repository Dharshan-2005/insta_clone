{
  "name": "instagram-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docker compose up -d --build --wait",
    "stop": "docker compose down",
    "reset": "docker compose down --volumes",
    "logs": "docker compose logs -f",
    "test": "npm ci --prefix tests --no-audit --no-fund --loglevel=error && node tests/e2e.mjs"
  }
}
